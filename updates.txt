# views

	# admin
		- tiles/list_view	
		
	# store/signin - check top of the page to uncomment https

# models	

	
# controllers
	- cart/check_session - very funny function uncomment on live one


# css
	
	
# config
	- constatns / social links, asset path, check for others	
	

# new sess var
	
	
	
# database - done

ALTER TABLE `tiles` ADD `category` TINYINT NOT NULL DEFAULT '1' COMMENT '1: male section, 2: female section' AFTER `sort_order`;
ALTER TABLE `tiles` ADD `btn_name` VARCHAR(255) NOT NULL AFTER `name`, ADD `caption` TEXT NOT NULL AFTER `btn_name`;
ALTER TABLE `tiles` ADD `btn_visibility` TINYINT NOT NULL DEFAULT '0' COMMENT '1: visible, 0: not visible. Default 0' AFTER `category`;



# Friday New updates
	- check feature products multi carousel
	- instagram model header name changed from h3 to h4
	- controller/lightspeed.php   customer state thing